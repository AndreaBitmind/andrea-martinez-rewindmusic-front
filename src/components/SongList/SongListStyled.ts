import styled from "styled-components";

export const SongListStyled = styled.div`
  padding-top: 15px;

  .songs-list {
    list-style: none;
    padding-left: 0;
    min-width: 390px;
    display: grid;
    gap: 12px;
  }
`;
