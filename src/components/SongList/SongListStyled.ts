import styled from "styled-components";

export const SongListStyled = styled.div`
  padding: 10px 10px;

  .songs-list {
    list-style: none;
    padding-left: 0;
    max-width: 390px;
    display: grid;
    gap: 12px;
  }
`;
