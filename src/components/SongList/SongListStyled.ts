import styled from "styled-components";

export const SongListStyled = styled.div`
  .songs-list {
    list-style: none;
    padding-left: 0;
    min-width: 220px;
    display: grid;
    gap: 20px;
  }
`;
