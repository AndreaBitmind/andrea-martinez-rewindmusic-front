import styled from "styled-components";

export const SongListStyled = styled.div`
  padding: 15px 0 20px 0;

  .songs-list {
    list-style: none;
    padding-left: 0;
    max-width: 390px;
    display: grid;
    gap: 12px;
  }
`;
